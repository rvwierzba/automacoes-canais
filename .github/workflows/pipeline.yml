# .github/workflows/pipeline.yml
name: Pipeline de Automação de Vídeos

on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: '0 5 * * *'  # Executa todos os dias às 05:00 UTC (~2h da manhã no Brasil)
    - cron: '0 19 * * *' # Executa todos os dias às 19:00 UTC (~16h da tarde no Brasil)

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Passo 1: Checkout do Código
      - name: Checkout Repository
        uses: actions/checkout@v3

      # Passo 2: Configurar Python
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # Passo 3: Instalar ImageMagick e Fonts DejaVu
      - name: Install ImageMagick and Fonts
        run: |
          sudo apt-get update
          sudo apt-get install -y imagemagick fonts-dejavu
          convert -version
        shell: bash

      # Passo 4: Listar Versões do MoviePy (usando pip index versions)
      - name: List Available MoviePy Versions
        run: |
          pip install --upgrade pip
          pip index versions moviepy
        shell: bash

      # Passo 5: Instalar Dependências
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --no-cache-dir -r requirements.txt --index-url https://pypi.org/simple
        shell: bash

      # Passo 6: Verificar Instalação das Bibliotecas
      - name: Verify Installations
        run: |
          python -c "import moviepy; print('MoviePy version:', moviepy.__version__)"
          python -c "import google.generativeai as genai; print('Google Gemini Library Imported Successfully')"
          python -c "import yaml; print('PyYAML Imported Successfully')"
          convert -version
        shell: bash

      # Passo 7: Exportar Variáveis de Ambiente (Secrets) e Criar client_secret.json
      - name: Export Environment Variables and Create client_secret.json
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TIKTOK_EMAIL: ${{ secrets.TIKTOK_EMAIL }}
          TIKTOK_PASSWORD: ${{ secrets.TIKTOK_PASSWORD }}
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          YOUTUBE_CHANNEL_ID: ${{ secrets.YOUTUBE_CHANNEL_ID }}
          CLIENT_SECRET_JSON: ${{ secrets.CLIENT_SECRET_JSON }}
        run: |
          echo "GEMINI_API_KEY=${GEMINI_API_KEY}" >> $GITHUB_ENV
          echo "TIKTOK_EMAIL=${TIKTOK_EMAIL}" >> $GITHUB_ENV
          echo "TIKTOK_PASSWORD=${TIKTOK_PASSWORD}" >> $GITHUB_ENV
          echo "YOUTUBE_API_KEY=${YOUTUBE_API_KEY}" >> $GITHUB_ENV
          echo "YOUTUBE_CHANNEL_ID=${YOUTUBE_CHANNEL_ID}" >> $GITHUB_ENV
          echo "${CLIENT_SECRET_JSON}" > client_secret.json
        shell: bash

      # Passo Adicional: Listar Arquivos para Depuração
      - name: List Files for Debugging
        run: |
          echo "Listando arquivos no diretório raiz:"
          ls -la
          echo "Listando arquivos no diretório 'configs/':"
          ls -la configs/
          echo "Listando arquivos no diretório 'assets/':"
          ls -la assets/
          echo "Listando arquivos no diretório 'data/':"
          ls -la data/
          echo "Listando arquivos no diretório 'audio/':"
          ls -la audio/
          echo "Listando arquivos no diretório 'generated_videos/':"
          ls -la generated_videos/
        shell: bash

      # Passo 8: Executar o Script Principal
      - name: Run Main Pipeline Script
        run: |
          python main.py
        shell: bash

      # Passo 9: Upload de Logs
      - name: Upload Logs
        uses: actions/upload-artifact@v3
        with:
          name: logs
          path: |
            main.log
            run_pipeline.log
            criar_video.log
            upload_youtube.log
            upload_tiktok.log
